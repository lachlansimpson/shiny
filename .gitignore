.DS_Store
.Rproj.user
.Rhistory
.Rprofile
*.o
*.so
/src-i386/
/src-x86_64/
shinyapps/
README.html
.*.Rnb.cached
/_dev/
.sass_cache_keys

# TypeScript
/node_modules/
.cache
coverage/
madge.svg

# Shiny uses npm (package-lock.json). Ignore Bun's lockfile so a stray
# `bun install` doesn't reintroduce it; migrating to Bun would be a separate,
# deliberate change to the build scripts and CI.
bun.lock
bun.lockb


# GHA remotes installation
.github/r-depends.rds
.claude/settings.local.json
/docs/
.context
